# Backend URL Configuration

# Local Development (uses Vite proxy)
# Leave empty for local development - Vite proxy handles it
VITE_BACKEND_URL=

# Production (single server - backend serves frontend)
# Leave empty if backend serves the built frontend
VITE_BACKEND_URL=

# Separate Backend Server (if deploying frontend and backend separately)
# VITE_BACKEND_URL=https://your-backend-url.com
